language: node_js
node_js:
  - "8"
  - "10"
  - "12"

script:
  - npm run lint
  - npm run validate --prod
  - npm run test-debug && nyc report --reporter=text-lcov | coveralls

branches:
  only:
    - master
